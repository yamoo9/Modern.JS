'use strict';

// ES6 -> Babel -> ES5 변환(Convert)

// let, const
{
  var _heySomeMessage = function _heySomeMessage(msg) {
    return msg || 'hi there';
  };
  var this_is_local_variable = '지역 변수';
}

heySomeMessage('hello JS');