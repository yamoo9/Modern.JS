{
  "private": true,
  "scripts": {
    "start": "npm run babel:w | npm run serve",
    "serve": "./node_modules/.bin/lite-server",
    "babel": "babel es6 -d js",
    "babel:w": "npm run babel -- -w"
  },
  "devDependencies": {
    "babel-preset-env": "^1.2.2",
    "lite-server": "^2.3.0"
  },
  "dependencies": {
    "bulma": "^0.4.0",
    "jquery": "^3.2.1"
  }
}
